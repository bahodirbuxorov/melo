// lib/core/theme/app_gradients.dart
import 'package:flutter/material.dart';

class AppGradients {
  static const main = LinearGradient(
    colors: [Color(0xFF6C63FF), Color(0xFF937BFF)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );
}
