import 'package:iconly/iconly.dart';

class AppIcons {
  static const call = IconlyLight.call;
  static const message = IconlyBold.message;
  static const profile = IconlyLight.profile;
  static const password = IconlyLight.password;
}
